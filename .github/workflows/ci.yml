name: CI Jardin Facile

# Déclenchement à chaque push sur n'importe quelle branche
on: [push]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # Récupération du code
      - name: Checkout code
        uses: actions/checkout@v4

      # Configuration de l'environnement Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # Installation 
      - name: Installation des dépendances
        run: npm install

      # Test simulé avec echo 
      - name: Exécution des tests
        run: |
          echo "Démarrage des tests unitaires..."
          npm test -- --watchAll=false
          echo "Pipeline terminée avec succès (CI verte) !"